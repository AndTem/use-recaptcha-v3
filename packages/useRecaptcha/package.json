{
  "name": "rc-use-recaptcha-v3",
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "pika-pack build",
    "tests": "jest",
    "lint": "eslint --debug src/**/**.ts",
    "lint:types": "tsc --noEmit",
    "release": "yarn run lint && yarn run lint:types && yarn run tests && yarn run build && npm publish ./pkg"
  },
  "@pika/pack": {
    "pipeline": [
      [
        "@pika/plugin-ts-standard-pkg"
      ],
      [
        "@pika/plugin-build-umd",
        {
          "sourcemap": false
        }
      ],
      [
        "@pika/plugin-build-node",
        {
          "sourcemap": false
        }
      ],
      [
        "@pika/plugin-build-web",
        {
          "sourcemap": false
        }
      ],
      [
        "pika-plugin-clean-dist-src"
      ]
    ]
  },
  "dependencies": {
    "react-script-hook": "1.0.17"
  },
  "peerDependencies": {
    "react": "^16.8.0",
    "react-dom": "^16.8.0"
  }
}
